apiVersion: apps/v1
kind: Deployment
metadata:
  name: blazor-ui-deployment
spec:
  selector:
    matchLabels:
      app: blazor-ui-deployment
  template:
    metadata:
      labels:
        app: blazor-ui-deployment
    spec:
      containers:
      - name: blazor-ui-deployment
        image: music-albums-rating/blazor-ui

        resources:
          limits:
            memory: "128Mi"
            cpu: "500m"
        ports:
        - containerPort: 80
